import AboutLayout from "@/components/Layout/AboutLayout";
import SiteIntro from "@/components/About/SiteIntro";

const SiteIntroPage = () => {
  return (
    <AboutLayout>
      <SiteIntro />
    </AboutLayout>
  );
};

export default SiteIntroPage;
