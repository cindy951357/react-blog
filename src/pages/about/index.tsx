import AboutLayout from '@/components/AboutLayout';

const About = () => {
  return (
    <AboutLayout>
      {/**meaning that default is site-intro page*/}
      About
    </AboutLayout>
  );
};

export default About;
